---
description: Cancel a running background OpenCode delegation job
argument-hint: "<job-id>"
---

Cancel a running background opencode delegation job.

Run: `oc-job cancel $ARGUMENTS`

Report whether the job was cancelled or already finished/not running.
